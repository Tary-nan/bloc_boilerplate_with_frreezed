name: todos_repository_local_storage
description: An app-agnostic data source that can be used by all implementations

environment:
  sdk: '>=2.12.0 <3.0.0'


dependencies:
  flutter:
    sdk: flutter
  todos_core_repository:
    path: ../todos_core_repository
  rxdart: 
  shared_preferences: ^2.0.8

dev_dependencies:
  test:
  flutter_test:
    sdk: flutter
  mockito:
